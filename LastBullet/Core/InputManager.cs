using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Input;

namespace LastBullet.Core
{
    public static class InputManager
    {
        // public static MouseState Mouse => Mouse.GetState();
        // public static KeyboardState Keyboard => Keyboard.GetState();
        // public static Vector2 MousePosition => new Vector2(Mouse.X, Mouse.Y);
        // public static bool IsLeftClick => Mouse.LeftButton == ButtonState.Pressed;
    }
}